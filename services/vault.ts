/**
 * Vault - Normalized State Management for IIIF Resources
 *
 * ⚠️ RE-EXPORT SHIM - This file re-exports from the decomposed FSD location.
 * Direct imports from here are deprecated. Prefer importing from:
 *   @/src/entities/manifest/model/vault
 *
 * This shim maintains backward compatibility during the FSD migration.
 *
 * @see src/entities/manifest/model/vault/ for the actual implementation
 */

// Re-export everything from the decomposed vault module
export * from '@/src/entities/manifest/model/vault';
